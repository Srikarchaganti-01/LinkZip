<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Hey From Server</h1>
    <% if (locals.id) { %>
     <p>Url Genarated : http://localhost:8001/url/<%= id %></p>
    <% } %>
    <div>
      <form method="post" action="/url">
        <label>Enter your Original Url</label>
        <input type="text" name="url" placeholder="https://example.com" />
        <button type="submit">Gen Short Url</button>
      </form>
    </div>
    <div>
      <% if (locals.urls) { %>
       <table>
        <thead>
          <th>S.no</th>
          <th>ShortId</th>
          <th>Original Url</th>
          <th>Visits</th>
          
        </thead>
        <tbody>
          <% urls.forEach((url,index) => { %>
           <tr>
            <td><%= index+1 %></td>
            <td><%= url.shortId %></td>
            <td><%= url.orgId %></td>
            <td><%= url.history.length %></td>  
          </tr>
          <% }) %>
        </tbody>
      </table>
      <% } %>
    </div>
  </body>
</html>
